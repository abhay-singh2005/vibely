import { createContext, useState } from 'react';
export const AuthContext = createContext(null);
export function AuthProvider({ children }) { const [user, setUser] = useState(null); const login = (nextUser) => setUser(nextUser || { username: 'yourname', name: 'Your Name', avatar: 'https://images.unsplash.com/photo-1500648767791-00dcc994a43e?auto=format&fit=crop&w=400&q=80', bio: 'Traveling, creating, and sharing everyday moments.' }); const register = login; const logout = () => setUser(null); const updateUser = (updates) => setUser((current) => ({ ...current, ...updates })); return <AuthContext.Provider value={{ user, login, register, logout, updateUser }}>{children}</AuthContext.Provider>; }
