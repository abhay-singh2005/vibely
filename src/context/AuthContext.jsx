import { createContext, useState } from 'react';
export const AuthContext = createContext(null);
export function AuthProvider({ children }) { const [user, setUser] = useState(null); const login = (nextUser) => setUser(nextUser || { username: 'srk.king', name: 'Shah Rukh Khan', avatar: 'https://images.unsplash.com/photo-1500648767791-00dcc994a43e?auto=format&fit=crop&w=400&q=80', bio: 'King of romance, stories, and unforgettable cinema magic.' }); const register = login; const logout = () => setUser(null); const updateUser = (updates) => setUser((current) => ({ ...current, ...updates })); return <AuthContext.Provider value={{ user, login, register, logout, updateUser }}>{children}</AuthContext.Provider>; }
